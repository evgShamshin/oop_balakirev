class Loader:
    @classmethod
    def json_parse(cls):
        return ""


Loader.json_parse()

# ld.json_parse(Loader)

# Loader.json_parse(ld)

res = Loader.json_parse()

# ld.json_parse()

# res = ld.json_parse()